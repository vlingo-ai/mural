# Gate 7 英语验收续办 — 2026-09-24

状态：已部署，尚未验收通过。按已接受开发基准收尾 Gate 7，不把全部后续架构重构追加为本轮门槛。普通话开发及测试继续暂停。

## 已有样本汇总

来源：[英语计时诊断记录](2026-09-23-english-timing-diagnostic.md)。下表是历史观测汇总，不是今天重新测试。

| 项目 | 已有证据 | 限制/缺测 |
| --- | --- | --- |
| 首音 | 两次浏览器能量候选：15,646、14,366 ms；n=2，样本中位数 15,006 ms，最差观测 15,646 ms | 非物理声学测量，少量样本不报告代表性 p95；两次诊断版本不同，第二次增加能量事件 |
| 打断 | 两次人工确认功能成功；第二次测得能量候选 280 ms，n=1 | 第一次无有效计时；单点不作为延迟分布或因果证明 |
| 重连 | 已有断网恢复、恢复后新回复与声音、Stop 回 Idle 的人工功能证据 | 缺完整计时报告；不可用人工“约 10 秒”代替浏览器各阶段时间戳 |
| 结算 | 最后记录会话 closed，23,000 ms，provider final，reservation settled | 当前数据库需重新检查；历史两条非 final 仍为运营对账未确认，不追加扣费 |
| 资源/流量 | LiveKit 项目级汇总与 VPS 空闲快照已有记录 | 无与有效通话时窗对齐的 CPU/RSS 采样峰值；累计量不冒充单会话用量 |

## 今天的只读复核

- VPS 部署工作树 HEAD：`525925e63922efab7a9a25d9920b18e622388748`。
- 公网 API `/healthz` 返回 `ok:true`、`hostedVoice:true`，guest minutes/live payments 均关闭。
- 社区帖子读取工具访问失败，不能断言无新回复，也不能据此豁免真实 Cloud 配额拒绝门槛。
- 用户 VPS 终端复核：`active-sessions.sh` 成功且无结果行，即未发现 state 非 closed 的 Mural 会话；测试账户 `balance_ms=93000`、`reserved_ms=0`，与昨日最后记录一致。这不是对 LiveKit 残留房间的独立核验。
- 未创建会话、未调用付费供应商、未部署或重启服务。最新 provider 费用及 LiveKit 使用量尚待复核；93 秒为 Mural 分钟余额，不代表外部供应商美元预算。

## OpenAI 仪表盘截图观察

用户提供的 2026-09-24 截图显示：项目 `mural-dev`、`All API keys`、`Last 7 days`，
图表横轴可见 Sep 17 至 Sep 24；`Total Spend` 为 USD 0.94。
右侧 `September spend / Personal` 显示 USD 0.94 / USD 100.00，另显示 69 tokens、1 request。
不把右侧 100 美元当成余额或本轮测试授权，也不从该请求计数推算实时语音会话总数。
昨日记录采用 Last 30 days、显示 USD 0.92，筛选范围不同，因此数字差 0.02 美元
不能直接归因于最后一次 23 秒测试或认定为最终增量账单。该截图证明当前筛选下的显示值，
不证明所有费用已最终入账。仍需同范围复核及 LiveKit 最新用量后才决定是否开展下一次付费样本。
Usage 的消费额不减 USD 90 充值。

随后用户补充同项目 `mural-dev`、`All API keys`、`Last 30 days` 截图，横轴可见
Aug 25 至 Sep 24，Total Spend 同为 USD 0.94。相较昨日同类 30 天筛选的 USD 0.92，
显示总额增加 USD 0.02；滚动窗口跨日且金额仅显示到美分，不能据此精确归因到某个会话，
也不代表最终账单。已补齐当前 30 天筛选读数，但 LiveKit 最新使用量仍未取得。

## LiveKit 仪表盘截图观察

用户截图显示正确项目 `vlingo-speaking-live-staging`、`Past 30 days`。
Agents 区域的 `AGENT SESSION MINUTES` 与 `CONCURRENT AGENT SESSIONS` 均为
`No data for the selected time range.`，不是明确数值 0；不能据此证明无计费、无活跃 Agent，
或 Build 额度充足。截图未包含 participant minutes、上下行流量及套餐已用/总额度。
当前原因未确定，不将空图直接归因于自托管 Worker 或数据延迟。下一步取得实时连接用量和额度面板。

后续同项目、Past 30 days 截图显示 `WebRTC participant minutes` 为 105 分钟，
与昨日记录的显示数值相同，但昨日记录为不同时间窗口，且聚合/取整/入账情况未知，
不能据此认定没有新增用量。下方为 `AVERAGE CONCURRENT CONNECTIONS` 历史平均曲线，
不是实时连接数，不用于确认当前零连接。105 分钟为项目累计参与者用量，不等于本轮
Mural 用户授权的 15 分钟会话额度；尚缺上下行流量和套餐当前已用/总额度。

随后同项目、Past 30 days 的 Data transfer 截图显示 Total upstream 7.92 MB、
Total downstream 6.87 MB。这些是项目所选时间窗口的累计显示值，不是单次会话流量，
也不是 VPS 网络接口采样。当前已取得参与者分钟数及上下行流量，套餐当前已用/总额度仍待核对。

Rooms 区域后续截图显示 `TOTAL ROOM SESSIONS=21`、`AVERAGE ROOM SIZE=2`，
筛选仍为 Past 30 days。平均房间时长数值未出现在截图中，不填补推算；
上述均为历史聚合，不代表当前正在运行 21 个房间或现有剩余额度。

项目 Overview 截图显示 `CONCURRENT AGENT SESSIONS=0`、`AGENTS DEPLOYED=1`，
目标 Agent 卡片 `CONCURRENT SESSIONS=0`、`WORKERS=1`；平台状态为
`ALL SYSTEMS OPERATIONAL`。这是截图观察时的状态，不是本项目端到端功能验证，
也不单独证明没有无 Agent 的残留房间。`PROJECT DATA REGION` 显示
`European Union (Frankfurt)`，不将该数据区域字段等同于实际媒体路由或 Worker 所在区域。
侧栏 Settings 尚未展开，套餐/额度入口及剩余额度仍未确认。

随后 Billing 截图确认 `CURRENT PLAN=Build`，`NEXT INVOICE=$0.00`；
`BANDWIDTH TODAY=0 MB`、九月带宽显示 `0 GB`。这些是界面显示读数，
不据此否定 Usage 中已显示的 7.92/6.87 MB 流量，也不作为最终零费用或额度充足证明。
侧栏已明确显示 `Quota and limits` 入口，具体限额仍待该页核对；未点击 Upgrade 或变更套餐。

Quota and limits 截图随后确认 Build 限额固定，表格使用量列为过去 7 天峰值，不是实时剩余额度：

| 项目 | 限额 | 页面显示过去 7 天峰值 |
| --- | --- | --- |
| Concurrent participants | 100 | 2 |
| API requests per minute | 10,000 | 5 |
| Agents deployed on LiveKit Cloud | 1 | 0 |
| Concurrent agent sessions | 5 | 0 |
| Concurrent Egress requests | 2 | 0 |
| Total Ingresses | 2 | 0 |

参与者并发和请求速率历史峰值未接近限额；不需要为本次单会话测试提高这两项容量。
Agent 相关历史峰值 0 不替代 Overview 的当前 Worker 注册读数，也不用于证明不存在 Agent 历史用量。
截图未给出月度剩余分钟数，不将此表当成月度分钟余额。没有修改限额、升级、购买或制造拒绝。
该页不能证明 CreateRoom/CreateDispatch 拒绝路径通过，真实 Cloud 拒绝证据/最终豁免仍待处理。
下一步先取得 VPS 空闲资源基线，准备有界通话期间采样；不能用空闲快照代替通话采样。

## 今日音频设备

用户确认输出改为 AirPods，输入仍为昨日的 Wireless Mic RX。今日样本标记为此设备组合，
不把昨日 Mac mini 扬声器的本机校准直接视作当前组合已通过，也不无条件混合延迟样本。
先运行现有 local-audio-calibration.html 非计费校准，检查实际听到测试音和处理后麦克风能量；
尚未收到今日校准结果。测试期间保持输入、输出和音量不变，浏览器实际麦克风选择须与用户配置一致。

用户随后确认 AirPods 听到本机测试音，并提供校准 JSON：AudioContext 为 running，
阈值 0.012。quiet 62 个采样，超阈值比例 0%，RMS P95/最大值 0.0009/0.0012；
仅播放测试音 100 个采样，麦克风超阈值比例 0%，RMS P95/最大值 0.0003/0.0004；
边播放边说话 125 个采样，超阈值比例 26%，RMS P95/最大值 0.0835/0.1148。
测试音 RMS P95 为 0.0856、超阈值比例 99%；播放期间检测到 2 次本地起音候选，
0.001/0.002/0.003/0.005/0.012 各备选阈值均为 2 次。现有阈值的本机校准通过，
不需降低阈值；这是合成音、当前设备组合的可用性检查，不是实际 Agent 打断延迟或重连验收。
未启动 LiveKit 或模型会话。今日资源基线及通话期间采样仍待执行。

## 空闲基线和有界资源采样准备

用户终端最新快照前的时间标记为 `2026-09-24T02:21:08Z`（命令前时间，不是各容器精确采样时刻）：

| 容器 | Docker CPU | Docker memory usage |
| --- | --- | --- |
| Edge | 0.00% | 14.8 MiB |
| API | 0.04% | 47.09 MiB |
| Worker | 0.60% | 459.3 MiB |
| Gateway | 0.04% | 66.66 MiB |
| PostgreSQL | 2.56% | 37.41 MiB |

五个容器的 Net I/O 均显示 0B/0B，Compose 配置均为 host network；不将此值当作真实零流量。
这仍是空闲快照，Docker memory usage 不冒充 RSS。新增独立验收工具
[`capture-resources.sh`](capture-resources.sh)，对明确的五个容器采集 CPU/内存和进程 RSS 求和，
并读取主机网卡字节计数、可用内存，保留每次采集起止 UTC 时间。
进程 RSS 求和可能重复计算共享页；网卡流量包含 SSH 等非测试流量，不与 Cloud 单会话流量混同。
每轮采集后等待两秒，实际间隔包含命令耗时；输出采样最大值，不声称连续峰值。
不收集进程参数、环境变量、会话正文或容器日志，不发起 provider 请求。

脚本已通过本机及 VPS `bash -n`，上传至
`/home/vlingo-admin/gate7-capture-resources-20260924.sh`（mode 600），双方 SHA-256 为
`a2ea0f32af2e1e9d2d446be2241df85d11923882ac3ee9133c4f104f2e42fb11`。
尚未验证运行采样结果；待用户以 sudo 启动独立 systemd 临时任务，最多采样十分钟并设置
650 秒运行保护，断开 Mac 网络不影响 VPS 采样。此任务不自动开始或停止真实会话；
实际付费会话仍受独立测试时限和预算约束。未改变运行应用、配置、镜像或数据库。

用户终端确认采样任务在 `2026-09-24T02:25:04Z` 启动，unit 为
`vlingo-gate7-resources-20260924.service`，输出目录
`/var/tmp/vlingo-gate7-resources.nDd8Up`。启动日志不等于有效数据证据，
开始付费样本前仍需核对完成采样行及 RSS 数据；本记录此时尚未包含新的真实会话结果。
随后只读 `systemctl show` 发现 `ActiveState=failed`、`SubState=failed`、
`Result=exit-code`、`ExecMainStatus=1`。因此未放行付费测试；脚本尚未通过运行验证。
普通 SSH 用户不能读取该系统任务日志，已改请用户以 sudo 输出该采样 unit 的有限日志及 metadata，
不索取密码、不改变日志访问权限。具体失败原因待日志确认。

用户随后提供的 journal 确认失败原因为 Docker 返回
`Couldn't find PID field in ps output`：原脚本 `docker top -eo rss` 缺少 Docker 过滤进程所需 PID 列。
原任务于 `2026-09-24T02:25:06Z` 退出，不能作为完成的通话资源采样。
已修正为 `-eo pid,rss`，校验表头和数值后仅累计第二列 RSS；PID 不写入采样文件。
增加首个完整采样、每十个完整采样及最终完成数量的无正文进度输出。
本地语法检查和五项解析 fixtures（多进程合计、零 RSS、缺 PID、空进程集、非法 RSS）通过。
确认失败 unit 已停止、远端旧脚本无其他修改后上传修复，双方 SHA-256 为
`8fdbf19339365b54ec03d28f005fb81f553b2ca6a7dabadcff70c89bb2f4b17d`，VPS 语法检查通过。
真实 Docker 运行验证仍待用户执行 15 秒空闲试采样；尚未放行新的付费会话。

15 秒真实 VPS 空闲试采样随后通过：目录
`/var/tmp/vlingo-gate7-resources.HWESnK`，首个完整样本时间
`2026-09-24T02:30:02Z`，`COMPLETED_SAMPLES=4`、`EXIT_CODE=0`。
证明修复后的完整采集循环可在当前五个容器运行，不等于通话负载验收。
下一步启动新的独立长采样 unit `vlingo-gate7-resources-20260924-r2`，不复用失败 unit。

用户终端确认 r2 于 `2026-09-24T02:31:15Z` 启动，目录
`/var/tmp/vlingo-gate7-resources.Hjb1Pi`；`02:31:38Z` 附近只读 systemctl 检查
为 active/running，ExecMainStatus=0。采样窗口预计到约 `02:41:15Z`，并有 650 秒保护。
在已授权的 Gate 7 范围内安排一次英语观测：从 Start 起最多 60 秒，最多一次断网，
离线直到 Connecting 或最多 15 秒即恢复，恢复后确认新回复和声音再 Stop；失败即停止、不自动重试。
设备固定 AirPods 输出、Wireless Mic RX 输入。尚未收到实际执行结果，不能记为验收通过。

## 本次英语重连观测（用户已结束会话）

用户报告显示 Connecting、自动恢复 Active、收到新回复、Stop 后 Idle。
浏览器报告 Stop 在 56,478 ms，未超过本次 Start 起 60 秒操作时限；
该时间不是数据库扣费时长，也未独立证明服务端已完成关闭。

| 观测 | 浏览器相对时间/时长 |
| --- | --- |
| initial-media-ready | Start 后 8,097 ms |
| recovery-detected | Start 后 27,698 ms |
| recovery-media-ready | Start 后 35,378 ms |
| 检测到恢复状态 → 媒体 ready | 7,680 ms |
| 恢复后的首个声音能量候选 | Start 后 42,806 ms |
| 检测到恢复状态 → 首个声音候选 | 15,108 ms |
| 媒体 ready → 首个声音候选 | 7,428 ms |
| stop-requested | Start 后 56,478 ms |

核对 TimingRecorder 定义后确认以上恢复时长均从 recovery-detected 起算，
不等于恢复 Wi-Fi 起算；报告未含 browser-offline/browser-online，
`offlineToDetectionMs=null`，物理断网至检测延迟缺测，不能记为零。
`firstAudioMs=42806` 与 post-recovery-audio 是同一事件；没有恢复前的声音候选，
故排除于正常、未受断网影响的首音延迟样本集合。需用户澄清断网前是否实际听到 Agent，
当前不能推断为 Agent 初始静音、蓝牙故障或检测器漏报。
`interruptions=[]`，此次未提供打断计时样本，不判为打断失败。
仅新增一个恢复计时样本，不报告代表性 p95；恢复后声音延迟也包含用户何时开口等因素。
当前记录为人工重连流程通过、附有受限计时证据，数据库 final/预留释放和通话时窗资源数据仍待核对。

用户补充：断网前是否听到声音不确定，“好像没有”。因此保持初始首音缺测/不纳入正常首音样本，
不把不确定听感写成确定的初始静音故障；不为此立即追加付费测试。

随后 VPS 查询确认本次最新测试会话 `5f536da6-e189-4a3b-917d-6119cf5e83a6`，
created_at 为 `2026-09-24T02:35:12.464221Z`，state=closed、close_reason=user_requested，
observed_ms=charged_ms=49000、provider_usage_final=true、reservation_state=settled、used_ms=49000。
测试账户余额从 93000 降至 44000 ms，reserved_ms=0；全局 active-sessions 查询成功且无结果行。
本次内部最终用量与预留结算通过，未出现新的非 final 记录；这不是 OpenAI 最终美元账单。
资源采样 r2 在此核对时仍为 active/running，待自动结束后收取文件并按会话时窗汇总。

用户再次补充“似乎断网前没有听到声音”。因此本次只证明断网后的连接恢复及新回复，
不单独证明原本正常播放的声音跨断网恢复；也不将不确定听感升级为已确认故障。

## 资源数据完整性及初步汇总

r2 已正常结束，metadata 为 samples=146、exit_code=0、finished_utc=`2026-09-24T02:41:19Z`。
VPS 打包文件 `/home/vlingo-admin/gate7-resources-20260924-r2.tar.gz` 与下载副本
`/private/tmp/vlingo-gate7-analysis.8H2NUG/gate7-resources-20260924-r2.tar.gz` SHA-256 一致：
`5c8f42dac7fe53071911c41b3730d06b1ca9a59a8c13ebd303e6044446bea2e2`。
已检查归档仅含六个预期普通数据文件，未含密钥或正文。
校验 146 个 begin 各对应一个 end、五个 Docker stats 行和五个进程 RSS 行；
总计 Docker stats/RSS 各 730 行。首样本开始 `02:31:15.241Z`、末样本结束 `02:41:17.032Z`，
相邻样本起始间隔中位数 4.133 秒、最大 4.211 秒，未发现断网导致的采样缺口。

先取 `02:35:00Z–02:36:30Z` 作为包围本次会话的宽窗口，22 组采样，实际起始点从
`02:35:02.642Z` 至 `02:36:29.560Z`。这是暂定前后留余量窗口，
尚待数据库 close_requested_at 对齐，不能称为精确的 active-only 通话指标。

| 容器 | 该窗口 Docker CPU 采样最大值 | Docker 内存采样最大值 MiB | 进程 RSS 求和采样最大值 MiB |
| --- | --- | --- | --- |
| Edge | 0.32% | 15.18 | 49.75 |
| API | 10.83% | 41.63 | 98.10 |
| Worker | 8.02% | 500.80 | 1571.70 |
| Gateway | 20.44% | 79.70 | 82.29 |
| PostgreSQL | 4.05% | 44.85 | 169.68 |

窗口内主机 MemAvailable 最低 2195.27 MiB（约 2.14 GiB）。各指标最大值可能发生于不同时刻；
进程 RSS 求和包含重复共享页，不能将 Worker 的 1571.70 MiB 当成独占内存或与 Docker memory 相加。
不由单次低负载观测推断容量上限或排除全部性能问题。
该宽窗口 eth0 累计计数差为 RX 3,896,287 字节、TX 3,647,433 字节，包含非测试流量；
不是 Cloud 单会话流量。整个采样时窗 eth0 RX/TX 分别增加 4,060,763/4,519,189 字节。

## 当前待收尾（替代下方原执行顺序的已完成准备步骤）

- 已完成本次功能观察、单样本恢复计时、内部 final/结算和资源包收取；未新增付费会话。
- 查询本次 created_at / close_requested_at，进一步对齐资源时窗。
- 上述时间查询随后完成，按下节精确服务器窗口汇总；不再需要为资源采样重开会话。
- 本次结束后的 Cloud 用量及 OpenAI 费用仍需核对；测试前 USD 0.94 不当作测试后总额。
- 原始错误/请求统计的明确分母和真实 Cloud 拒绝证据/最终豁免仍待收尾。
- 正常首音已有两个历史候选，本次不纳入；断网检测耗时仍为缺测，打断仍只有历史单候选。
- 后续若希望把完整的已发声→断网→恢复体验补测为通过，需先评估剩余 44 秒和原授权范围，
  不自动重试或加额度。今天对断网前声音的不确定性明确留在最终报告中。

## 服务器窗口对齐后的资源结果

VPS 确认本次会话 created_at=`2026-09-24T02:35:12.464221Z`、
close_requested_at=`2026-09-24T02:36:06.140Z`、state=closed。
创建到关闭请求间隔约 53.676 秒，不是 active-only 时长；不同于浏览器 Start→Stop 56.478 秒，
也不同于最终 observed/charged 49 秒，三者定义不同，不强行改为相同值。

仅选择完整采集轮次的 begin/end 都位于该服务器窗口内的样本，共 12 组，
首组开始 `02:35:15.055Z`，末组结束 `02:36:02.738Z`。
以下取代宽窗口作为本次主要资源汇总，窗口边界与采样间隙仍可能漏掉瞬时峰值：

| 容器 | Docker CPU 采样最大值 | Docker 内存采样最大值 MiB | 进程 RSS 求和采样最大值 MiB |
| --- | --- | --- | --- |
| Edge | 0.32% | 15.18 | 49.75 |
| API | 10.49% | 41.56 | 98.04 |
| Worker | 8.02% | 500.10 | 1570.45 |
| Gateway | 0.05% | 66.67 | 82.29 |
| PostgreSQL | 4.05% | 44.85 | 169.68 |

主机最低 MemAvailable 为 2195.27 MiB，当前采样未显示明显资源耗尽。
宽窗口中 Gateway 20.44% 的最大值未出现在这些完整的会话内轮次中，
不能把它当作本次语音会话的 Gateway CPU 峰值。
取创建时间之前最近、关闭请求之后最近的 eth0 读数，网络包围窗口为
`02:35:10.923Z–02:36:08.876Z`，RX 增量 3,882,624 字节，TX 增量 3,599,003 字节。
该差值仍包含 VPS 其他网络活动和少量窗口外时间，不代表单会话 Cloud 计费流量。

本次恢复功能观察（带初始声音不确定性）、恢复计时、内部结算和资源汇总已记录；
还需测试后 provider/Cloud 仪表盘读数、错误统计定义/分母及 Cloud 拒绝证据或最终豁免。
不追加付费测试，Gate 7 尚未整体通过。

## 本次测试后的费用观察

用户提供测试后 OpenAI Usage 截图：项目 `mural-dev`、All API keys、Last 30 days，
地址栏筛选为 startDate=2026-08-25、endDate=2026-09-24，Total Spend 仍显示 USD 0.94。
这只是显示金额暂未变化，不证明本次 49 秒会话没有 provider 费用或费用已全部入账；
不把增量记为已确认的 USD 0.00。最终美元费用仍待对账，不再启动新的付费样本。
本次测试后的 LiveKit 参与者分钟、房间数、上下行流量与当前 Agent 并发读数仍待取得。

随后用户提供 LiveKit 同项目、Past 30 days 截图：WebRTC participant minutes 为 107 分钟，
相较本次测试前 105 分钟，显示增加 2 个参与者分钟。此为项目级聚合显示差值，
不替代内部 observed/charged 49 秒，不据此断言单会话精确计费时长或把 Mural 扣费改为两分钟。
测试后房间数、上下行流量和当前 Agent 并发读数仍待取得。

用户后续同一 Usage 页面流量截图显示 Total upstream=8.61 MB、Total downstream=7.5 MB。
该裁剪图未包含日期选择器；按前后连续页面上下文暂归为本次 Past 30 days 观察，
不声称独立核验了该截图日期范围。相对测试前 7.92/6.87 MB，显示差值分别为 +0.69/+0.63 MB，
是项目聚合且取整后的显示差，不认定为单会话精确计费字节数。
测试后房间数和 Overview 当前 Agent 并发数尚待取得。

后续 Rooms 截图显示 Total room sessions=22（测试前 21，显示增加 1）、
Average room size=2、Average room duration=8 分钟。该图日期选择器未入镜，
按连续 Usage 页面上下文记录，不独立确认筛选。平均时长是历史房间聚合显示，
不等于本次 49 秒最终用量或 53.676 秒服务器创建至关闭请求间隔，不据此推断本次仍持续运行。
测试后 Overview 当前 Agent 并发数仍待取得。

用户随后反馈 `No data for the selected time range.`，同时页面上下文仍为 `/usage`。
将其记录为历史统计区域无数据，不视为 Overview 当前并发 0；测试后当前 Agent 并发仍待确认。
已提供项目 `/overview` 直接入口，要求读取顶部 `CONCURRENT AGENT SESSIONS` 数字，
若仍不可得则保留“Cloud 当前并发未核实”，不重复请求整个 Usage 页。

用户随后进入项目 `/overview`，明确确认顶部当前 `CONCURRENT AGENT SESSIONS=0`。
证据来源为用户读数确认（不是工具直接读取或新截图）。结合此前数据库 state=closed、
provider_usage_final=true、reservation=settled、reserved_ms=0，本次停止后的会话状态核对完成。
这不独立证明不存在无 Agent 的残留房间，也不等同于后续架构中持久资源清理缺陷已修复。

## 本次收尾状态

- 已取得：带初始声音限制的恢复观察及单样本计时；49 秒内部 final/结算；余额 44 秒、预留 0；
  146 组完整资源采样及服务器窗口内 12 组汇总；Cloud 参与者分钟 107、房间数 22、
  上下行 8.61/7.50 MB、用户确认当前 Agent 并发 0。
- 尚待：本次 OpenAI 美元费用最终对账（显示仍为 0.94，不能把新增费用记为零）；
  明确错误统计分母与汇总；真实 Cloud 配额拒绝证据或最终明确豁免；验收报告对缺测的正式处理。
- 初始声音不确定、offlineToDetection 缺测与少量计时样本限制继续保留；不自动追加付费测试。
- 当前仍为已部署、英语 Gate 7 尚未整体通过，不把停止状态确认等同于全部资源已清理。

## Cloud 真实配额拒绝测试的条件豁免授权

本节保留当时安排；后续已收到实质回复，以下等待安排由[当日明确豁免决定](#cloud-waiver-decision)替代，不再执行。

2026-09-24 用户明确要求：“cloud 配额，再等 2 天社区回复，如无，就豁免”。
本轮英语 staging 在 9 月 26 日最后检查确认无实质回复时，真实 Cloud CreateRoom/CreateDispatch
配额拒绝测试可直接登记为用户授权豁免，无需再次征询；保留本地 429→数据库结算替代证据及限制，
不记成真实 Cloud 拒绝测试通过，也不自动豁免其他验收项。截止前状态仍为等待回复。
若存在实质回复，评估其安全性与成本，不擅自开展付费测试；无法访问不等于无回复。

已更新原 heartbeat `livekit`（未新建）：Asia/Shanghai 9 月 25 日与 26 日 10:55 检查，
最多两次。只有实质回复通知；无回复时安静，到期记录条件判断并结束任务。
脚本无法核实页面内容时如实记为核实失败，不伪称满足豁免条件。
该等待不阻塞继续整理错误统计、资源和计时限制、费用对账；今天不再追加付费样本。

## API 错误计数准备（非计费）

已检查 API Diagnostics 和 Fastify onResponse/error handler：request_failed 后不再写 request_completed，
reference 可用于去重；不把 provider 事件与请求事件混加为请求数。
新增 [`summarize-api-errors.py`](summarize-api-errors.py)，只从 stdin 解析指定日志窗口，输出
已识别事件计数、请求数、4xx/5xx、handler failures、包含/排除 health 的错误率与缺失/重复计数。
不输出 reference、operation、正文、日志原行或错误内容；分母为空输出 null 并失败退出，
冲突重复记录失败退出，不能伪称 0% 错误。测试窗口计划为 UTC 02:35:00–02:36:30，
与资源宽窗口一致，包含建立/停止前后，不冒充只属于目标会话的请求或整个系统错误率。
本地六项行为检查通过；VPS 上传脚本 `/home/vlingo-admin/gate7-summarize-api-errors-20260924.py`
权限 600，双方 SHA-256 为 `5f8abc84a89461b2f7d0b634c58f22f1524874c451d96d7e5aea23e6a8782b47`。
待用户执行只读日志管道取得实际汇总；尚未报告 API 错误率。

## API 错误统计结果

用户终端已返回指定 UTC 02:35:00–02:36:30 窗口的真实日志计数。
首末已识别事件时间分别为 `02:35:01.984Z` 和 `02:36:22.729Z`。

| 指标 | 结果 |
| --- | --- |
| 去重后 API 请求数（含健康检查） | 39 |
| 排除 GET /healthz 后请求数 | 30 |
| HTTP 4xx / HTTP 5xx / handler failures | 0 / 0 / 0 |
| 本窗口 API HTTP 错误率（含健康检查） | 0/39 = 0% |
| 本窗口 API HTTP 错误率（不含健康检查） | 0/30 = 0% |
| voice_active / voice_close_requested / voice_closed | 1 / 1 / 1 |
| 重复请求 / 无法解析行 / 未识别事件 | 0 / 0 / 0 |

分母有效，计数检查通过。仅代表该 API 容器、该 90 秒窗口内已落日志的请求，
含测试前后请求，不是整个 Gate 7、Worker/Gateway、浏览器、媒体链路的错误率；
不能据此将“初始声音不确定”或预期网络中断当作已排除的体验问题。
本次请求统计、内部结算及资源汇总已补齐，不再为这些指标开新付费会话。
现阶段主要未收尾项：计时缺测及有限样本的明确验收结论、测试后 OpenAI 最终费用对账，
以及等待 9 月 26 日满足条件后登记 Cloud 拒绝豁免（或采用实质回复提供的安全方案）。

## 收尾验收矩阵（以本节为当前汇总，之前逐步记录保留）

此表汇总计时、资源与对账收尾，不替代 Gate 7 的完整功能清单或新增通过结论。

| 收尾项 | 已有结果 | 当前结论 / 下一步 |
| --- | --- | --- |
| 正常首音候选 | 历史 15,646 / 14,366 ms，n=2；算术样本中位数 15,006 ms，最差观测 15,646 ms | 汇总完成；诊断版本不同，不是同条件性能基准。9 月 24 日 42,806 ms 受断网影响，排除 |
| 自然打断 | 历史人工功能成功；有效能量候选 280 ms，n=1 | 汇总完成；单样本中位数和最差观测均为 280 ms，但没有分布或最坏情况保证 |
| 重连 | 历史英语功能恢复证据；今日检测到恢复状态→媒体 ready 7,680 ms，→声音候选 15,108 ms，n=1 | 单样本各自中位数/最差观测等于该值；缺物理断网→检测、恢复 Wi-Fi→可用计时；用户最终确认断网前没有听到声音，本次不证明已有声音跨断网恢复 |
| CPU / 内存 / RSS / VPS 流量 | 146 组完整采样；服务器创建→关闭请求窗口内 12 组；主机 MemAvailable 最低约 2.14 GiB | 有界观测汇总完成，不是容量/压力测试；RSS 求和非独占内存，网卡差值含背景流量 |
| API 请求错误 | 指定 90 秒窗口，排除 health 0/30，包含 health 0/39；无法解析/未识别均 0 | 本窗口统计通过；不代表 Worker、Gateway 或媒体链路无错误 |
| 内部结算与停止 | 本次 closed / provider final / settled，49 秒；余额 44 秒、预留 0；用户确认 Overview Agent 并发 0 | 本次核对通过；历史两条非 final 仍保留运营对账未知，不追加扣费 |
| Cloud 用量 | 参与者 107 分钟、房间 22、上下行 8.61/7.50 MB | 项目级观察已记录，不冒充单会话计费量；缺数据区域不填零 |
| OpenAI 费用 | 同项目、同起止日期 USD 0.98，较 USD 0.94 增加 USD 0.04；用户确认没有其他项目并行使用 | staging 运营对账将显示增量 USD 0.04 归因本次测试；依据为截图和用户确认，保留美分精度及历史补记限制，非逐请求账单核验 |
| 真实 Cloud 拒绝 | 本地 429→真实数据库结算替代证据已存在 | WAIVED，未实测；9 月 24 日收到社区回复后用户明确授权，仅限本轮英语 staging；见后文决定 |
| 缺测与小样本处理 | 上述限制已列明；用户 9 月 24 日明确接受建议并要求保留记录、不影响推进 | 仅本轮英语 staging 例外已接受；不是完整性能验收通过 |

### 已接受：仅限本轮英语 staging 的计时限制

2026-09-24 用户明确回复“接受建议，保留记录，不影响推进”。因此不为补齐本轮计时立即追加
付费会话，将现有功能证据与下列观测限制一并接受用于本轮英语 staging 收尾。
这是已授权的验收例外，不是宣称低延迟、性能达标或生产就绪：

1. 首音仅保留两个跨诊断版本历史候选；打断、重连各一个计时样本，不报告代表性 p95。
2. 今日物理断网至检测、恢复 Wi-Fi 至可用缺测；用户最终确认今日断网前没有听到声音。
   历史恢复功能证据继续有效，但今日样本不独立证明正常播放中的媒体跨断网恢复。
3. 上述缺测不得改填零或用人工回忆补齐；初始无可闻声音的现象已由用户确认，原因尚未定位，不据此确定故障组件，也不声称已修复。

在最终报告中将对应项目记为“有限观测已记录；缺测获 staging 例外接受”，
不是“完整性能验收通过”。独立的费用对账与 Cloud 门槛不受此决定影响。
后续跨端重连/可靠性工作应保留这些测量缺口：用同一浏览器单调时钟记录可观测的网络事件、
恢复检测、媒体 ready 与实际声音候选；事件不可观测时仍为 null。补测必须先确认初始音频可闻，
再中断网络，并按固定设备/诊断版本分组；设定目标和样本计划后另行授权付费测试。

用户同时补充：“断网前是否听到声音事宜，当做确认处理”。这句话未明确确认听到还是未听到，
因此先保留其原意并请求方向澄清，不擅自将此前“似乎未听到”的记录改成确定的正向或负向证据。
无论随后澄清为何，人工听感确认不能补出缺失时间戳；计时例外已生效，澄清不阻塞费用及 Cloud 收尾。

### 下一步费用对账交接（不启动会话）

- 下一次费用核对使用项目 `mural-dev`、All API keys，并保留覆盖 9 月 24 日测试的固定日期范围
  及页面时区/实际起止日期；同时保留截图读取时间。不要只凭 Last 30 days 跨日数值做减法。
- 如可导出同一范围的费用汇总，只保留日期、项目标识、金额/币种等必要汇总字段；不提供 API key、
  cookie、授权头或会话正文。项目总额可能含其他调用，不强行归因到本次 49 秒会话。
- 将 provider 费用观察、Mural 内部最终用量和 Cloud 聚合值分别列示；OpenAI Usage 不减 USD 90 充值。
  余额法才需调整充值，不能与 Usage 法混用。
- 当前只有 USD 0.94 的界面显示证据，未取得最终对账材料；不得因等待时间经过或数值未变自动标记完成。
  若仍只有非最终显示值，保留待对账，不继续消耗剩余 44 秒来验证账单。

本轮收尾整理只修改文档，没有部署、重启、修改验收脚本或新建付费会话。

### OpenAI 更新截图核对结果（2026-09-24，替代上方 USD 0.94 待更新状态）

用户明确提供“OpenAI 对账已更新，可对账”的截图。直接可见项目 `mural-dev`、
`All API keys`、`Last 30 days`，地址栏 `startDate=2026-08-25&endDate=2026-09-24`，
与此前 USD 0.94 截图起止日期相同。Total Spend 为 USD 0.98；September spend 同为 USD 0.98。
图表悬停标签含 UTC；截图未提供精确读取时刻或费用逐笔导出，不补写时间。

| 核对项目 | 结果 |
| --- | --- |
| 此前相同项目/筛选显示 | USD 0.94 |
| 最新相同项目/筛选显示 | USD 0.98 |
| 两次显示金额差 | USD 0.04 |
| 原 Gate 7 OpenAI 预算上限 | USD 2.00 |
| 以当前项目显示总额比较上限 | 0.98 < 2.00，显示差额 USD 1.02；不是可直接追加消耗的授权 |

该费用观察与本次 49 秒内部 final/settled 结果分别记录。USD 0.04 是同范围项目总额的显示差。
用户随后明确确认“没有其他的项目在并行使用”，要求据此归因。接受该补充作为运营证据，
本轮 staging 对账将显示增量 USD 0.04 归因于本次测试，不再以其他项目并行使用作为归因阻碍。
证据来源为同范围截图及用户确认，未独立核验逐请求账单；无并行使用本身不排除历史费用补记，
且两个美分精度显示值相减不能证明原始精度金额恰好为 USD 0.040000。
USD 90 充值不从 Usage 中扣除；右侧 USD 100.00 不替代本轮 USD 2.00 授权上限。
69 tokens / 1 request 不作为全部实时语音用量的分母。

本轮 staging 的更新后项目级费用与预算核对已完成，证据层级为用户提供的 Usage 截图，
不是正式最终发票；本次测试的运营归因已按上述用户确认记录，不冒充逐会话原始账单匹配。此前等待“更新后的费用读数”已满足，
不再因旧 USD 0.94 未变化而阻塞推进；后续如有补记，仍应如实追加，不把两条历史非 final 改成已对账。
没有新增付费调用。当时明确待办为 Cloud 实测拒绝证据或 9 月 26 日生效的条件豁免（现已由后文明确决定替代），
以及据此形成最终 Gate 7 报告；计时限制已获 staging 接受，声音观察的最终澄清见下节。

### 用户最终澄清：断网前未听到声音

2026-09-24 用户明确回复“断网前，没有听到声音”。此前“好像没有”“似乎没有”及待澄清记录
作为过程历史保留；当前事实结论更新为用户确认未听到，而不是仍不确定，也不是确认听到。
这与首次远端声音能量候选出现在恢复后相符，但不足以确定模型未生成音频、客户端未播放、
设备路由异常或其他具体原因；没有源码/日志因果证据前不归因。

本次证据边界为：Connecting → Active、恢复后有新回复和声音、Stop → Idle，以及服务端最终结算。
本次不证明断网前已有正常音频，也不证明正在播放的音频跨断网恢复。
`firstAudioMs=42806` 仍排除于未受断网影响的正常首音统计，不补写断网前首音时间。
保留“初始阶段未听到声音，原因未定位”为后续可靠性调查项；此次事实澄清不撤销用户已接受的
staging 限制、不追加付费测试，不将此现象写成已修复或完整体验验收通过。

<a id="cloud-waiver-decision"></a>

## 真实 Cloud 拒绝：社区回复复核与明确豁免（2026-09-24）

已通过论坛公开 JSON 只读核实[第 2 楼回复](https://community.livekit.io/t/safe-way-to-test-real-roomservice-quota-rejection-on-build-without-exhausting-shared-limits/2106/2)：
作者 Muhammad Usman Bashir，发布时间 2026-09-23 13:52:33 UTC（北京时间 21:52:33），
post ID 7319。账户字段 staff/admin/moderator 均为 false；不将其意见写成 LiveKit 官方承诺。

回复建议保留本地拒绝测试、考虑联系支持临时调整 staging 限额，也提出用 API 请求突发触发限流。
本轮不采用突发方案：API 速率拒绝不等同并发/资源配额拒绝，且不能保证不影响共享免费项目。
[官方限额文档](https://docs.livekit.io/deploy/admin/quotas-and-limits/)说明免费项目共享额度与限制；
其 Server API 1,000 requests/min 与本项目截图的 10,000/min 不一致，不能擅自用任一值设计压测。
临时下调 Build 项目限额仍只是社区建议，未获得支持方可执行的隔离方案或批准；
本项目真实配额拒绝的具体状态码和错误结构也没有通过本次阅读得到实测确认。

用户随后明确回复“采纳建议”，接受本轮豁免并停止自动检查。决定如下：

- **结果：WAIVED（未实测），不是 PASS。** 生效于 2026-09-24，仅覆盖当前 Phase 5.5B 英语 staging Gate 7 的真实 CreateRoom/CreateDispatch 配额拒绝测试；随本轮签结结束，后续发布、5.5C 与生产不得自动继承。
- 理由：已有本地 429→真实数据库结算证据，尚无经确认的安全、低影响 Cloud 拒绝触发办法；不为补此证据耗尽共享配额、购买容量或干扰其他会话。
- 本次为收到实质回复后的新明确授权，替代原“9 月 26 日确认无回复才豁免”安排；不能记录为无人回复或条件自动满足。
- `livekit` 自动化已通过应用工具删除，返回 `deleteStatus=deleted`；不再执行 9 月 25/26 日每日检查。本轮没有发送社区回复、调整配额、创建会话或执行付费/突发测试。
- 保留本地拒绝、错误映射及结算测试证据和真实 Cloud 行为未知这一限制。本轮未重跑运行时测试，未修改或部署应用代码。
- 其他已接受限制和未知项继续保留；最终 Gate 7 报告及签结仍待办，不能据此宣布整个 Phase 5.5B 完成。B1–B7 加固工作可以继续，不新增为原 Gate 7 门槛。

本次文档维护同步总计划、验收索引、runbook 和发布验证方案；差异空白、相对文件链接与代码围栏检查通过。
可复用规则：条件豁免前提变化时重新判断授权；社区建议不等于供应商支持承诺；限流与配额拒绝分别归类。

## 初始执行顺序（准备阶段历史计划，完成状态以上文为准）

1. 只读核对全局未关闭会话、测试账户余额和预留。历史最后值为 93,000 ms / reserved 0，并非今天确认值。
2. 核对 OpenAI 同项目/日期筛选下最新费用和 LiveKit 使用量；昨天 USD 0.92 有入账延迟，不是最终费用。USD 90 充值不算消费；只有按余额变动推算消费时才调整充值，Usage 消费额不再减 90。
3. 在既有授权余量明确后，安排一个有上限的英语重连观测，同时采集无正文的计时报告、CPU/RSS/流量采样、结算结果；没有余量则先请求额外授权，不自动充值。
4. 采样覆盖整个通话窗口并记录间隔；输出采样最大值，不称为连续峰值。区分浏览器断网事件→检测、恢复信号→媒体 ready、恢复信号→首个声音候选，缺失值为 null。
5. 核实社区实质回复；安全低影响方案才考虑真实拒绝测试，不耗尽共享配额、不购买容量。无法安全实现时记录用户最终豁免决定，不伪称真实测试通过。
6. 汇总通过/豁免/未通过、样本数、费用窗口、错误率分母和剩余风险，证据齐备前不宣布 Phase 5.5B 完成。

当前继续保留 staging 公网 SSH 和 API/Worker 共用 LiveKit 项目密钥对的既有安全例外；本次复核不改变它们。
